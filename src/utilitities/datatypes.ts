export interface headerContent {
  title: string;
  about: string;
  language: string;
}

export interface menuContent {
  title: string;
  prototype: string;
  frontend: string;
  backend: string;
  other: string;
}
